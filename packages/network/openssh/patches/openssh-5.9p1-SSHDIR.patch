diff -Naur openssh-5.9p1/Makefile.in openssh-5.9p1.patch/Makefile.in
--- openssh-5.9p1/Makefile.in	2011-08-05 22:15:18.000000000 +0200
+++ openssh-5.9p1.patch/Makefile.in	2011-11-23 04:40:59.645952757 +0100
@@ -30,7 +30,7 @@
 SSH_PRIVSEP_USER=@SSH_PRIVSEP_USER@
 STRIP_OPT=@STRIP_OPT@
 
-PATHS= -DSSHDIR=\"$(sysconfdir)\" \
+PATHS= -DSSHDIR=\"/run/cache/ssh\" \
 	-D_PATH_SSH_PROGRAM=\"$(SSH_PROGRAM)\" \
 	-D_PATH_SSH_ASKPASS_DEFAULT=\"$(ASKPASS_PROGRAM)\" \
 	-D_PATH_SFTP_SERVER=\"$(SFTP_SERVER)\" \
diff -Naur openssh-5.9p1/pathnames.h openssh-5.9p1.patch/pathnames.h
--- openssh-5.9p1/pathnames.h	2011-05-29 13:39:38.000000000 +0200
+++ openssh-5.9p1.patch/pathnames.h	2011-11-23 04:59:23.803731233 +0100
@@ -18,6 +18,11 @@
 #define SSHDIR				ETCDIR "/ssh"
 #endif
 
+#ifndef CONFIGDIR
+#define CONFIGDIR			ETCDIR "/ssh"
+#endif
+
+
 #ifndef _PATH_SSH_PIDDIR
 #define _PATH_SSH_PIDDIR		"/var/run"
 #endif
@@ -34,8 +39,8 @@
  * Of these, ssh_host_key must be readable only by root, whereas ssh_config
  * should be world-readable.
  */
-#define _PATH_SERVER_CONFIG_FILE	SSHDIR "/sshd_config"
-#define _PATH_HOST_CONFIG_FILE		SSHDIR "/ssh_config"
+#define _PATH_SERVER_CONFIG_FILE	CONFIGDIR "/sshd_config"
+#define _PATH_HOST_CONFIG_FILE		CONFIGDIR "/ssh_config"
 #define _PATH_HOST_KEY_FILE		SSHDIR "/ssh_host_key"
 #define _PATH_HOST_DSA_KEY_FILE		SSHDIR "/ssh_host_dsa_key"
 #define _PATH_HOST_ECDSA_KEY_FILE	SSHDIR "/ssh_host_ecdsa_key"
